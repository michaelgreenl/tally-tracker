name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    name: Static checks and unit tests
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      HUSKY: 0
      JWT_SECRET: ci-jwt-secret-for-unit-tests
      POSTGRES_URL: postgresql://template_user:template_pass@localhost:5432/template_db
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          package-manager-cache: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate Prisma Client
        run: bun --filter=@tally/core run generate:client

      - name: Lint TypeScript and Vue
        run: bun run lint

      - name: Lint client styles
        run: bun --filter=client run lint:style:check

      - name: Typecheck
        run: bun run typecheck

      - name: Unit tests
        run: bun run test:unit

      - name: Component tests
        run: bun run test:component

      - name: Build
        run: bun run build

  server-integration:
    name: Server integration with Postgres
    runs-on: ubuntu-24.04
    needs: checks
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: tally_tracker_test
          POSTGRES_USER: tally_test_user
          POSTGRES_PASSWORD: tally_test_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U tally_test_user -d tally_tracker_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      HUSKY: 0
      NODE_ENV: test
      TALLY_TEST_DB_RESET: 1
      POSTGRES_URL: postgresql://tally_test_user:tally_test_password@127.0.0.1:5432/tally_tracker_test
      JWT_SECRET: ci-jwt-secret-for-server-integration
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          package-manager-cache: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate Prisma Client
        run: bun --filter=@tally/core run generate:client

      - name: Apply committed migrations
        run: bun --filter=@tally/core run migrate:deploy

      - name: Check migration drift
        run: bun --filter=@tally/core run migrate:check

      - name: Run server integration tests
        run: bun --filter=server run test:integration

  e2e:
    name: Cypress with Postgres
    runs-on: ubuntu-24.04
    needs: checks
    timeout-minutes: 25
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: template_db
          POSTGRES_USER: template_user
          POSTGRES_PASSWORD: template_pass
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U template_user -d template_db"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      HUSKY: 0
      NODE_ENV: development
      PORT: 3000
      FRONTEND_URL: http://localhost:8100
      VITE_API_URL: http://localhost:3000
      POSTGRES_URL: postgresql://template_user:template_pass@localhost:5432/template_db
      JWT_SECRET: ci-jwt-secret-for-e2e
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          package-manager-cache: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate Prisma Client
        run: bun --filter=@tally/core run generate:client

      - name: Apply committed migrations and seed database
        run: |
          bun --filter=@tally/core run migrate:deploy
          bun --filter=@tally/core run db:seed

      - name: Run Cypress
        uses: cypress-io/github-action@v7
        with:
          install: false
          command: bun run test:e2e
          start: |
            bun run dev:server
            bun run dev:client
          wait-on: 'http://localhost:3000/health, http://localhost:8100'
          wait-on-timeout: 120
