# agents/adhoc/

- Purpose: for ad-hoc runs

- Planned ad-hoc runs go in `./queued/`
- Active ad-hoc runs go in `./active/`

- Ad-hoc runs do not require an assigned workflow
- Workflow/Agent can be assigned by user when requesting an ad-hoc run to be executed

